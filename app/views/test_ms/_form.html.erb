<%= form_for(test_m) do |f| %>
  <% if test_m.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(test_m.errors.count, "error") %> prohibited this test_m from being saved:</h2>

      <ul>
      <% test_m.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :comment %>
    <%= f.text_field :comment %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
